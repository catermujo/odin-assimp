# Odin bindings for Assimp (Open Asset Import Library)

To avoid submodules (for sanity reasons) the build script can clone the library for you.

```bash
# Windows
./build.bat
# Linux/Darwin
./build.sh
```

By default it avoids unnecessary dependencies and enables OpenUSD.
